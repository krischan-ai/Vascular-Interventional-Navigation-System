# 开发进度记录

> 日期：2026-06-17
> 当前提交：`f48f6ed feat: 新增路径平滑功能与VPP血管碰撞支持`
> 远程分支：`origin/main`（本地领先 2 个提交）

---

## 一、当前阶段结论

已完成四条核心后端链路：

### 1.1 VPP 路径规划链路（阶段一已完成）

```text
VPP 数据迁移
  -> 资产 manifest / targets 生成
  -> 资产结构校验
  -> 中心线图加载
  -> A* 路径规划
  -> FastAPI REST API
  -> 自动化测试验收
```

### 1.2 CathSim NavigationEngine 桥接链路（阶段二已完成）

```text
REST Session API
  -> SessionManager 会话管理
  -> NavigationEngine 环境封装
  -> CathSim MuJoCo step
  -> NavigationState 标准化状态
  -> 自动化测试验收（20 passed）
```

### 1.3 WebSocket 实时通信链路（阶段三已完成）

```text
WebSocket /ws/session
  -> WebSocketHandler 连接管理
  -> 实时控制输入（30Hz 限速）
  -> 状态推送 state_update
  -> 心跳 ping/pong 机制
  -> 自动化测试验收（15 passed）
```

### 1.4 B-spline 路径平滑（阶段四已完成）

```text
POST /api/v1/path/plan?smooth=true
  -> scipy.interpolate B-spline 拟合
  -> 曲率计算
  -> 平滑路径输出
  -> 自动化测试验收（12 passed）
```

### 1.5 V-HACD 真实血管碰撞检测（阶段五已完成）

```text
blood_vessels.vtk
  -> V-HACD 凸包分解
  -> 129 个 STL 碰撞几何
  -> MuJoCo XML phantom 定义
  -> 25 个 endpoint sites
  -> NavigationEngine VPP 支持
  -> 自动化测试验收（9 passed）
```

本阶段未迁移 Godot 可复用代码。

---

## 二、已完成内容

### 2.1 VPP 数据迁移工具

新增工具脚本：

| 文件 | 作用 |
|------|------|
| `tools/migrate_vpp_assets.py` | 将外部 VPP 数据迁移到 `data/vpp_assets/case_001` |
| `tools/validate_vpp_assets.py` | 校验迁移后的图、半径、标记点和派生文件 |
| `tools/build_target_map.py` | 从 `Endpoints.fcsv` 生成 `derived/targets.json` |

迁移来源：

```text
E:\文档\Project\centerline\vascular_path_planner_sim\data
```

迁移目标：

```text
data/vpp_assets/case_001/
├── manifest.json
├── raw/
│   ├── blood_vessels.vtk
│   ├── centerline_curves.vtk
│   ├── Endpoints.fcsv
│   ├── crossPoints.fcsv
│   └── intervenPoints.fcsv
├── graph/
│   ├── graph.json
│   └── node_radii.json
├── derived/
│   └── targets.json
└── mujoco/
    └── meshes/
```

注意：仓库 `.gitignore` 当前忽略 `data/`，因此迁移后的 VPP 数据存在本机，但默认不会提交到 Git。

### 2.2 后端规划内核

新增后端模块：

| 文件 | 作用 |
|------|------|
| `services/graph_loader.py` | 加载 VPP 图 JSON，转换为数值邻接表 |
| `services/path_planner.py` | 基于中心线图执行 A* 路径规划 |
| `services/coordinate.py` | LPS、MuJoCo、Godot 坐标转换工具 |

当前 `PathPlanner` 已支持：

- 加载 `graph/graph.json`
- 最近图节点吸附
- A* 搜索
- 返回路径点、路径长度、节点数、计算耗时

### 2.3 FastAPI REST 服务

新增服务入口：

| 文件 | 作用 |
|------|------|
| `services/main.py` | FastAPI 应用入口 |
| `services/schemas.py` | Pydantic 请求/响应模型 |

当前接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| `GET` | `/api/v1/health` | 服务健康状态 |
| `GET` | `/api/v1/assets/cases` | 列出已迁移 case |
| `GET` | `/api/v1/assets/cases/{case_id}` | 获取 case manifest |
| `POST` | `/api/v1/path/plan` | 执行路径规划 |

启动命令：

```powershell
python -m services.main
```

或：

```powershell
uvicorn services.main:app --host 0.0.0.0 --port 8000
```

### 2.4 依赖与测试

更新 `pyproject.toml`：

- 新增 `fastapi`
- 新增 `uvicorn[standard]`

新增测试：

| 文件 | 作用 |
|------|------|
| `tests/test_vpp_path_planner.py` | 单测图加载与 A* 路径规划 |
| `tests/test_services_api.py` | 单测 FastAPI health、case、path plan 接口 |

### 2.5 CathSim NavigationEngine 桥接

新增后端模块：

| 文件 | 作用 |
|------|------|
| `services/navigation_engine.py` | 封装 CathSim MuJoCo 环境，提供 `reset()`/`step()` |
| `services/session_manager.py` | 多会话管理，支持并发导航实例 |

`NavigationEngine` 核心功能：

- 封装 `cathsim.dm.make_dm_env` 创建环境
- `reset()` 返回 `NavigationState` 初始状态
- `step(delta_push, delta_rotate)` 执行物理仿真步
- `get_safety_status(state)` 返回安全级别
- 提取标准化状态字段：
  - `tip_position` - 导丝头端位置
  - `tip_direction` - 导丝头端方向向量
  - `velocity` - 头端速度
  - `contact_force` - 接触力
  - `episode_length` - 当前 episode 步数
  - `target_position` - 目标位置
  - `reward` / `done` - 强化学习信号

`SessionManager` 核心功能：

- 线程安全的会话创建/销毁
- 自动过期清理（默认 1 小时）
- 最大并发会话限制（默认 10）
- 会话元数据跟踪（创建时间、步数、episode 数）

### 2.6 Session REST API

新增接口：

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/session/start` | 创建导航会话 |
| `GET` | `/api/v1/session` | 列出所有活跃会话 |
| `GET` | `/api/v1/session/{id}` | 获取会话元数据 |
| `POST` | `/api/v1/session/{id}/step` | 执行仿真步 |
| `POST` | `/api/v1/session/{id}/reset` | 重置环境 |
| `DELETE` | `/api/v1/session/{id}` | 关闭会话 |

更新 `services/schemas.py`：

- `SessionStartRequest` / `SessionStartResponse`
- `NavigationStateResponse`
- `StepRequest` / `StepResponse`
- `ResetResponse`
- `SessionInfoResponse`
- `SafetyStatus` 类型定义

### 2.7 NavigationEngine 测试

新增测试：

| 文件 | 作用 |
|------|------|
| `tests/test_navigation_engine.py` | 单元测试 + MuJoCo 集成测试 |

测试类别：

- `TestNavigationStateDataclass` - 状态数据类单元测试
- `TestSessionManagerUnit` - SessionManager 单元测试（无需 MuJoCo）
- `TestSchemas` - Pydantic schema 验证测试
- `TestNavigationEngineIntegration` - NavigationEngine 集成测试（需 MuJoCo）
- `TestSessionAPIIntegration` - Session REST API 集成测试（需 MuJoCo）

### 2.8 WebSocket 实时通信

新增后端模块：

| 文件 | 作用 |
|------|------|
| `services/websocket_handler.py` | WebSocket 连接管理与消息处理 |

`WebSocketHandler` 核心功能：

- 连接生命周期管理（accept/close）
- 心跳机制：服务端每 5s 发送 ping，15s 无 pong 响应则断开
- 控制输入处理（30Hz 限速）
- 实时状态推送 `state_update`
- 会话管理消息：`session_start`, `session_stop`, `reset`
- 错误处理与响应

WebSocket 消息类型：

| 方向 | 类型 | 说明 |
|------|------|------|
| C→S | `session_start` | 创建会话 |
| C→S | `session_stop` | 关闭会话 |
| C→S | `control` | 控制命令 |
| C→S | `reset` | 重置环境 |
| C→S | `pong` | 心跳响应 |
| S→C | `session_started` | 会话已创建 |
| S→C | `session_stopped` | 会话已关闭 |
| S→C | `state_update` | 状态更新 |
| S→C | `ping` | 心跳请求 |
| S→C | `error` | 错误响应 |

WebSocket 端点：

| 路径 | 说明 |
|------|------|
| `/ws/session` | 实时控制与状态流 |

### 2.9 WebSocket 测试

新增测试：

| 文件 | 作用 |
|------|------|
| `tests/test_websocket.py` | 单元测试 + MuJoCo 集成测试 |

测试类别：

- `TestWebSocketHandlerUnit` - WebSocket 处理器单元测试（无需 MuJoCo）
- `TestWebSocketIntegration` - WebSocket 集成测试（需 MuJoCo）

### 2.10 B-spline 路径平滑

更新 `services/path_planner.py`：

- `smooth_path()` 方法：基于 scipy.interpolate.splprep 的 B-spline 平滑
- `_compute_max_curvature()` 方法：计算路径最大曲率
- `PathResult` 增加字段：`smooth_waypoints`, `smooth_length_mm`, `max_curvature`
- `plan()` 方法支持 `smooth` 参数

平滑算法特性：

- 基于累积弧长参数化
- 支持可调平滑因子 `smoothing_factor`（0.0 = 插值，>0 = 平滑）
- 自动计算输出点数（默认 2x 输入）
- 计算路径曲率用于安全评估

API 更新：

| 方法 | 路径 | 新增参数 |
|------|------|----------|
| `POST` | `/api/v1/path/plan` | `smooth: bool` |

响应新增字段：

- `smooth_waypoints` - 平滑后的路径点
- `smooth_length_mm` - 平滑路径长度
- `max_curvature` - 最大曲率 (mm⁻¹)

### 2.11 B-spline 测试

更新测试：

| 文件 | 作用 |
|------|------|
| `tests/test_vpp_path_planner.py` | 新增 `TestBSplineSmoothing` 类 |
| `tests/test_services_api.py` | 新增 `test_path_plan_with_smooth` |

测试用例：

- `test_smooth_path_basic` - 基本平滑功能
- `test_smooth_path_preserves_endpoints` - 端点保持
- `test_smooth_path_without_flag` - 无平滑时返回 None
- `test_smooth_path_direct_method` - 直接调用 smooth_path
- `test_smooth_path_too_few_points` - 点数不足报错
- `test_path_result_as_dict_with_smooth` - 字典序列化
- `test_path_plan_with_smooth` - API 端点测试

### 2.12 V-HACD 凸包分解

新增工具脚本：

| 文件 | 作用 |
|------|------|
| `tools/vhacd_decompose.py` | 将 blood_vessels.vtk 转换为 MuJoCo 碰撞几何 |

V-HACD 工具功能：

- 读取 VTK 血管表面模型
- 坐标变换 LPS (mm) → MuJoCo (m)
- V-HACD 凸包分解（使用 trimesh + vhacdx）
- 导出 STL 碰撞几何
- 生成 MuJoCo XML phantom 定义
- 从 Endpoints.fcsv 提取目标位置

生成资产：

```text
data/vpp_assets/case_001/mujoco/
├── case_001_vpp.xml        # MuJoCo phantom 定义
└── meshes/case_001/
    ├── visual.stl          # 可视化网格
    ├── hull_0.stl          # 碰撞凸包 0
    ├── hull_1.stl          # 碰撞凸包 1
    └── ... (共 129 个凸包)
```

XML 结构：

- 129 个碰撞几何（hull_0 ~ hull_128）
- 25 个 endpoint sites（从 Endpoints.fcsv 提取）
- 坐标系：米 (m)
- 碰撞几何类型：凸包 (convex hull)

### 2.13 NavigationEngine VPP 支持

更新文件：

| 文件 | 作用 |
|------|------|
| `src/cathsim/dm/components/phantom.py` | 支持外部 `assets_dir` 参数 |
| `src/cathsim/dm/env.py` | `make_dm_env` 增加 `assets_dir` 参数 |
| `services/navigation_engine.py` | 支持 VPP phantom 加载 |

NavigationEngine 新参数：

- `assets_dir` - 外部 phantom 资产目录路径

使用示例：

```python
engine = NavigationEngine(
    phantom="case_001_vpp",
    target="endpoints_1",
    assets_dir="data/vpp_assets/case_001/mujoco"
)
```

### 2.14 V-HACD 测试

新增测试：

| 文件 | 作用 |
|------|------|
| `tests/test_vhacd.py` | V-HACD 生成资产验证测试 |

测试类别：

- `TestVHACDGeneration` - 验证生成的 XML 和 STL 文件
- `TestVHACDTool` - 验证工具脚本存在
- `TestManifestIntegration` - 验证 manifest.json 集成

测试用例：

- `test_vpp_xml_exists` - XML 文件存在
- `test_visual_mesh_exists` - 可视化网格存在
- `test_hull_meshes_exist` - 凸包网格存在（>10 个）
- `test_xml_structure` - XML 结构正确
- `test_xml_sites_have_positions` - 站点位置有效
- `test_hull_count_matches_xml` - 凸包数量匹配

---

## 三、验收结果

### 3.1 自动化测试（阶段一）

执行命令：

```powershell
python -m pytest .\tests\test_services_api.py .\tests\test_vpp_path_planner.py -q
```

结果：

```text
5 passed
```

### 3.2 自动化测试（阶段二 - NavigationEngine）

单元测试（无需 MuJoCo）：

```powershell
python -m pytest tests/test_navigation_engine.py -v -m "not slow"
```

结果：

```text
tests/test_navigation_engine.py::TestNavigationStateDataclass::test_default_values PASSED
tests/test_navigation_engine.py::TestNavigationStateDataclass::test_as_dict PASSED
tests/test_navigation_engine.py::TestSessionManagerUnit::test_max_sessions_config PASSED
tests/test_navigation_engine.py::TestSessionManagerUnit::test_close_nonexistent_session PASSED
tests/test_navigation_engine.py::TestSessionManagerUnit::test_get_nonexistent_session PASSED
tests/test_navigation_engine.py::TestSessionManagerUnit::test_list_sessions_empty PASSED
tests/test_navigation_engine.py::TestSchemas::test_step_request_validation PASSED
tests/test_navigation_engine.py::TestSchemas::test_step_request_out_of_range PASSED
tests/test_navigation_engine.py::TestSchemas::test_session_start_request_defaults PASSED
9 passed
```

集成测试（需 MuJoCo）：

```powershell
MUJOCO_GL=glfw python -m pytest tests/test_navigation_engine.py -v -m slow
```

结果：

```text
tests/test_navigation_engine.py::TestNavigationEngineIntegration::test_engine_initialization PASSED
tests/test_navigation_engine.py::TestNavigationEngineIntegration::test_engine_reset PASSED
tests/test_navigation_engine.py::TestNavigationEngineIntegration::test_engine_step PASSED
tests/test_navigation_engine.py::TestNavigationEngineIntegration::test_engine_multiple_steps PASSED
tests/test_navigation_engine.py::TestNavigationEngineIntegration::test_engine_safety_status PASSED
tests/test_navigation_engine.py::TestSessionAPIIntegration::test_session_start_endpoint PASSED
tests/test_navigation_engine.py::TestSessionAPIIntegration::test_session_step_endpoint PASSED
tests/test_navigation_engine.py::TestSessionAPIIntegration::test_session_reset_endpoint PASSED
tests/test_navigation_engine.py::TestSessionAPIIntegration::test_session_list_endpoint PASSED
tests/test_navigation_engine.py::TestSessionAPIIntegration::test_session_close_endpoint PASSED
tests/test_navigation_engine.py::TestSessionAPIIntegration::test_session_not_found PASSED
11 passed
```

### 3.3 自动化测试（阶段三 - WebSocket）

单元测试（无需 MuJoCo）：

```powershell
python -m pytest tests/test_websocket.py -v -m "not slow"
```

结果：

```text
tests/test_websocket.py::TestWebSocketHandlerUnit::test_message_types_enum PASSED
tests/test_websocket.py::TestWebSocketHandlerUnit::test_control_data_validation PASSED
tests/test_websocket.py::TestWebSocketHandlerUnit::test_session_start_data_defaults PASSED
tests/test_websocket.py::TestWebSocketHandlerUnit::test_websocket_message_parsing PASSED
tests/test_websocket.py::TestWebSocketHandlerUnit::test_connection_state_defaults PASSED
5 passed
```

集成测试（需 MuJoCo）：

```powershell
MUJOCO_GL=glfw python -m pytest tests/test_websocket.py::TestWebSocketIntegration -v
```

结果：

```text
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_connect_disconnect PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_session_start PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_control_without_session PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_session_control_step PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_session_reset PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_session_stop PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_pong_response PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_invalid_message_type PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_invalid_control_params PASSED
tests/test_websocket.py::TestWebSocketIntegration::test_websocket_multiple_steps PASSED
10 passed
```

### 3.4 自动化测试（阶段四 - B-spline 平滑）

执行命令：

```powershell
python -m pytest tests/test_vpp_path_planner.py tests/test_services_api.py -v
```

结果：

```text
tests/test_services_api.py::test_health_reports_migrated_case PASSED
tests/test_services_api.py::test_case_manifest_endpoint_returns_graph_stats PASSED
tests/test_services_api.py::test_path_plan_endpoint_uses_vpp_graph PASSED
tests/test_services_api.py::test_path_plan_with_smooth PASSED
tests/test_vpp_path_planner.py::test_graph_loader_parses_vpp_adjacency PASSED
tests/test_vpp_path_planner.py::test_path_planner_finds_astar_path PASSED
tests/test_vpp_path_planner.py::TestBSplineSmoothing::test_smooth_path_basic PASSED
tests/test_vpp_path_planner.py::TestBSplineSmoothing::test_smooth_path_preserves_endpoints PASSED
tests/test_vpp_path_planner.py::TestBSplineSmoothing::test_smooth_path_without_flag PASSED
tests/test_vpp_path_planner.py::TestBSplineSmoothing::test_smooth_path_direct_method PASSED
tests/test_vpp_path_planner.py::TestBSplineSmoothing::test_smooth_path_too_few_points PASSED
tests/test_vpp_path_planner.py::TestBSplineSmoothing::test_path_result_as_dict_with_smooth PASSED
12 passed
```

### 3.5 自动化测试（阶段五 - V-HACD）

执行命令：

```powershell
python -m pytest tests/test_vhacd.py -v
```

结果：

```text
tests/test_vhacd.py::TestVHACDGeneration::test_vpp_xml_exists PASSED
tests/test_vhacd.py::TestVHACDGeneration::test_visual_mesh_exists PASSED
tests/test_vhacd.py::TestVHACDGeneration::test_hull_meshes_exist PASSED
tests/test_vhacd.py::TestVHACDGeneration::test_xml_structure PASSED
tests/test_vhacd.py::TestVHACDGeneration::test_xml_sites_have_positions PASSED
tests/test_vhacd.py::TestVHACDGeneration::test_hull_count_matches_xml PASSED
tests/test_vhacd.py::TestVHACDTool::test_tool_script_exists PASSED
tests/test_vhacd.py::TestVHACDTool::test_tool_has_main_function PASSED
tests/test_vhacd.py::TestManifestIntegration::test_manifest_has_mujoco_section PASSED
9 passed
```

全部测试汇总：**56 passed** (21 VPP/API + 20 NavigationEngine + 15 WebSocket + 9 V-HACD - 重复计数)

### 3.6 VPP 资产校验

执行命令：

```powershell
python .\tools\validate_vpp_assets.py
```

结果摘要：

```text
graph:
  nodes: 10561
  edges: 21120
  min_weight: 0.5251997714936855
  max_weight: 17.910244889206396

radii:
  nodes: 10561
  missing_graph_nodes: 0
  extra_radii_nodes: 0

markers:
  endpoints: 25
  cross_points: 26
  interven_points: 6

derived:
  targets_exists: true
```

### 3.6 REST 接口抽检

`GET /api/v1/health`：

```json
{
  "status": "ok",
  "version": "0.1.0",
  "vpp_ready": true,
  "cases": ["case_001"]
}
```

`GET /api/v1/assets/cases` 摘要：

```json
{
  "case_id": "case_001",
  "coordinate_system": "LPS",
  "unit": "mm",
  "graph_nodes": 10561,
  "graph_edges": 21120,
  "endpoints": 25
}
```

`POST /api/v1/path/plan` 抽检：

```text
start: Endpoints-24
end: Endpoints-25
node_count: 52
length_mm: 52.127
status: 200
```

---

## 四、当前未完成事项

### 4.1 目标点语义确认

`derived/targets.json` 已生成全部 endpoint，但以下别名仍未绑定：

```json
{
  "root": null,
  "bca": null,
  "lcca": null
}
```

原因：源 `Endpoints.fcsv` 标签为 `Endpoints-1` 到 `Endpoints-25`，没有直接包含 BCA/LCCA/root 等医学语义。需要后续通过可视化或人工确认后再写入别名。

### 4.2 真实血管碰撞转换

尚未执行：

```text
blood_vessels.vtk
  -> V-HACD
  -> convex STL hulls
  -> MuJoCo XML
  -> low_tort_vpp.xml
```

这是后续高风险任务，应在路径规划与 CathSim bridge 稳定后再做。

### 4.3 Godot 客户端

按当前决策，暂不迁移 `godot_vascular_sim` 中的 Godot 可复用代码。

---

## 五、Git 状态说明

已推送提交：

```text
4925be9 add vpp asset migration and planning api
```

推送目标：

```text
https://github.com/krischan-ai/cathsim
main -> origin/main
```

当前本地仍有一个未提交改动：

```text
M src/cathsim/dm/utils.py
```

该文件不是本阶段开发产生的改动，未纳入提交，未推送。

---

## 六、建议下一步

### 已完成：CathSim NavigationEngine 桥接（阶段二）

✅ 新增 `services/navigation_engine.py`
✅ 封装 `cathsim.dm.make_dm_env`
✅ 实现 `reset()`、`step(delta_push, delta_rotate)`
✅ 提取并规范化状态字段
✅ 新增 Session REST 接口
✅ 增加 smoke tests（20 passed）

### 已完成：WebSocket 实时通信（阶段三）

✅ 新增 `services/websocket_handler.py`
✅ 实现 WebSocket 端点 `/ws/session`
✅ 实时状态推送（30Hz 限速）
✅ 心跳 ping/pong 机制
✅ 连接状态管理
✅ 增加测试（15 passed）

### 已完成：B-spline 路径平滑（阶段四）

✅ 实现 `smooth_path()` B-spline 平滑方法
✅ 集成到 `PathPlanner.plan()` 方法
✅ 更新 API `POST /api/v1/path/plan` 支持 `smooth` 参数
✅ 计算平滑路径长度和最大曲率
✅ 增加测试（12 passed）

### 已完成：V-HACD 真实血管碰撞（阶段五）

✅ 新增 `tools/vhacd_decompose.py` V-HACD 凸包分解工具
✅ 生成 129 个 STL 碰撞凸包
✅ 生成 MuJoCo XML phantom 定义
✅ 更新 `cathsim.dm.components.phantom` 支持外部资产目录
✅ 更新 `cathsim.dm.env.make_dm_env` 支持 `assets_dir` 参数
✅ 更新 `NavigationEngine` 支持 VPP phantom 加载
✅ 增加测试（9 passed）

系统已形成五条闭环：

```text
REST control / WebSocket
  -> FastAPI Session / WebSocketHandler
  -> NavigationEngine
  -> CathSim MuJoCo step
  -> state response / state_update

路径规划 + 平滑
  -> A* 搜索
  -> B-spline 平滑
  -> 曲率分析
  -> smooth_waypoints

真实血管碰撞
  -> blood_vessels.vtk
  -> V-HACD 凸包分解
  -> MuJoCo XML phantom
  -> NavigationEngine VPP 支持
```

### 建议阶段六：端到端集成测试

1. 使用 VPP phantom 进行 MuJoCo 仿真测试
2. 验证碰撞检测准确性
3. 路径规划与仿真联动测试

---

## 七、阶段六执行记录：VPP Phantom 端到端集成测试

### 7.1 当前结论

阶段六已完成第一轮端到端验证：

```text
VPP MuJoCo XML
  -> dm_control MJCF physics compile
  -> CathSim make_dm_env reset/step
  -> NavigationEngine reset/step
  -> VPP phantom slow tests
```

验证结果表明，`case_001_vpp.xml` 及其 129 个 V-HACD 凸包不只是静态文件存在，也可以进入 CathSim/dm_control 环境执行物理仿真步。

### 7.2 修复内容

本阶段发现并修复了两个集成问题：

1. 高精度 `visual.stl` 不应注册进 MuJoCo XML

   原因：MuJoCo STL decoder 对单个 STL 面数有限制，高精度血管表面更适合保留给外部渲染器，不适合直接作为 MuJoCo mesh asset 编译。

   处理：

   - `tools/vhacd_decompose.py` 保留 `visual.stl` 文件导出
   - MuJoCo XML 只注册 `hull_*.stl` 碰撞凸包
   - 当前 `case_001_vpp.xml` 已移除 visual mesh/geom 引用

2. `Phantom` 需要兼容无 visual mesh 的 VPP XML

   处理：

   - `src/cathsim/dm/components/phantom.py` 的 `set_rgba()`、`set_scale()` 在找不到 visual mesh/geom 时跳过
   - `case_001_vpp.xml` 对应 `meshes/case_001/` 的目录命名已兼容

3. Python 依赖版本需要固定

   处理：

   - `pyproject.toml` 固定 `dm-control==1.0.14`
   - `pyproject.toml` 固定 `mujoco==2.3.7`
   - 补充 `pympler`、`pyvista`、`vhacdx`、`scipy`

   背景：`dm_control 1.0.41` 与 `mujoco 3.9.0` 在当前环境中触发 `flex_bandwidth` 索引不兼容；项目 `environment.yml` 原本使用的是 `dm-control==1.0.14` 与 `mujoco==2.3.7`。

### 7.3 新增测试

更新文件：

| 文件 | 作用 |
|------|------|
| `tests/test_navigation_engine.py` | 新增 `TestVPPPhantomIntegration` |

新增测试用例：

- `test_vpp_phantom_sites_load`
- `test_vpp_mjcf_physics_compiles`
- `test_navigation_engine_vpp_phantom_reset_and_step`

### 7.4 验收命令与结果

完整端到端验收命令：

```powershell
python -m pytest tests/test_vhacd.py tests/test_navigation_engine.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_websocket.py -v
```

实测结果（全部通过）：

```text
59 passed in 83.41s
```

分布：

| 测试文件 | 通过数 |
|---------|--------|
| `test_vhacd.py` | 9 |
| `test_navigation_engine.py` | 23 (9 单元 + 14 集成) |
| `test_services_api.py` | 4 |
| `test_vpp_path_planner.py` | 8 |
| `test_websocket.py` | 15 |

手动分层 smoke 已通过：

```text
MJCF compile:
  meshes: 129
  sites: 25
  physics geoms: 129

make_dm_env:
  reset ok
  observations: joint_pos, joint_vel
  step ok

NavigationEngine:
  reset ok
  target_position: [-0.97565564, -0.21722368, 0.25031761]
  step ok
  episode_length: 1
```

### 7.5 验收遗留修复（验收时追加）

验收阶段发现并修复两处遗漏：

1. **`manifest.json` 文件名错误**
   - `mujoco.xml` 字段值为 `mujoco/low_tort_vpp.xml`（文件不存在）
   - 修复为 `mujoco/case_001_vpp.xml`（正确路径）

2. **manifest 测试过弱**
   - `test_manifest_has_mujoco_section` 原来仅验证 key 存在
   - 补充 `assert xml_path.is_file()` 校验引用文件实际存在

---

## 八、进度总表（截至 2026-06-17）

| 阶段 | 内容 | 状态 | 测试覆盖 |
|------|------|------|---------|
| 阶段一 | VPP 数据迁移 + 资产管理 | ✅ 完成 | 5 passed |
| 阶段二 | CathSim NavigationEngine 桥接 | ✅ 完成 | 23 passed |
| 阶段三 | WebSocket 实时通信 | ✅ 完成 | 15 passed |
| 阶段四 | B-spline 路径平滑 | ✅ 完成 | 12 passed |
| 阶段五 | V-HACD 真实血管碰撞 | ✅ 完成 | 9 passed |
| 阶段六 | 端到端集成测试 | ✅ 完成 | 59 passed（全量） |
| 阶段七 | 风险评估 + 路径进度 | ✅ 完成 | 79 passed（全量） |
| 阶段七.5 | WebSocket 协议补全（state_batch / path_request / risk_score） | ✅ 完成 | 86 passed（全量） |
| 阶段八 | Godot 客户端基础场景 | ✅ 完成（首切片） | 后端契约回放校验 |
| 阶段九 | 实机联调：环境/性能/对齐根治 | ✅ 完成 | 73 passed（维护套件） |
| 阶段十 | VPP 真实血管导航 + 客户端接入（A 入口对齐 / B 按 phantom 选 GLB / C 路径可视化） | ✅ 完成（A/B/C） | 97 passed（维护套件） |
| 阶段十一 | 近距观察相机系统（第三人称跟随 + 第一人称内窥镜） | ✅ 完成 | 纯客户端，实机验证 |
| 阶段十二 | 血管入口高亮（介入血管入路：绿色入口 + 红色目标标记） | ✅ 完成 | 70 passed（维护套件） |
| 阶段十三 | VPP 全程导航：运动学循路模式（guided，导丝沿中心线必达目标） | ✅ 完成 | 104 passed（维护套件） |
| 阶段十四 | 导丝物理化外观（全长曲折 + 贴内壁 + 管状渲染消锯齿） | ✅ 完成 | 105 passed（维护套件） |
| 阶段十五 | 内窥镜管腔可见性（不透明内壁 + 自发光，弃头灯防爆白） | ✅ 完成 | 纯客户端，实机验证 |

---

## 九、阶段七开发计划：风险评估 + 路径进度集成

### 9.1 背景与动机

当前 `NavigationState` 已包含 `tip_position`、`contact_force`、`velocity` 等字段，  
但 `03-API与通信协议.md` 定义的 `state_update` 消息要求以下字段尚未实现：

| 字段 | 规格来源 | 当前状态 |
|------|---------|---------|
| `wall_distance` | §1.4 state_update | ❌ 缺失 |
| `curvature` | §1.4 state_update | ❌ 缺失 |
| `path_progress` | §1.4 state_update | ❌ 缺失 |
| `safety_status` | §1.4 state_update | ❌ 仅作为方法返回，未进 state |
| `path_deviation` | §3.3 风险评估体系 | ❌ 缺失 |
| `tip_quaternion` | §1.4 state_update | ❌ 缺失 |

Godot 客户端（阶段八）依赖这些字段驱动 HUD 显示和安全状态灯，  
必须在启动 Godot 工作之前在后端完成。

### 9.2 任务分解

#### 任务 A：`NavigationEngine` 注入 `PathPlanner`

**目标**：让 `NavigationEngine` 在 `reset()` 时接受一条规划路径，并在每步 `step()` 后实时计算路径进度和偏离量。

```python
# 新增接口
engine = NavigationEngine(
    phantom=”case_001_vpp”,
    target=”endpoints_1”,
    assets_dir=”...”,
    planned_path=smooth_waypoints,   # List[List[float]]，单位 m
)
state = engine.reset()
# state.path_progress  → [0.0, 1.0]
# state.path_deviation → 距规划路径最近距离 (m)
```

实现要点：
- `NavigationEngine.__init__` 增加 `planned_path: list[list[float]] | None` 参数
- 构建 `scipy.spatial.KDTree` 用于最近点查询
- `_extract_state()` 中计算 `path_progress`（沿路径弧长累积比例）和 `path_deviation`

#### 任务 B：`NavigationEngine` 计算 `wall_distance` 与 `curvature`

**目标**：每步返回导丝尖端的壁面距离和局部曲率。

```python
# NavigationState 新增字段
wall_distance: float = 0.0   # 单位 m（由 MuJoCo 接触几何估算）
curvature: float = 0.0       # 单位 m⁻¹（5 点差分法计算尖端曲率）
```

实现要点：
- `wall_distance`：从 MuJoCo `physics.data.contact` 中读取最近接触点距离；若无接触则设为大值（如 0.05m）
- `curvature`：维护尖端位置历史队列（5 帧），用 3 阶多项式或有限差分估算曲率

#### 任务 C：`NavigationState` 增加 `safety_status` 与 `tip_quaternion`

**目标**：将安全状态纳入 `NavigationState`，由 `NavigationEngine` 内部在 `_extract_state()` 中计算，而非由外部调用者手动调用 `get_safety_status()`。

```python
@dataclass
class NavigationState:
    ...
    wall_distance: float = 0.0
    curvature: float = 0.0
    path_progress: float = 0.0
    path_deviation: float = 0.0
    safety_status: str = “STANDBY”
    tip_quaternion: list[float] = field(default_factory=lambda: [0.0, 0.0, 0.0, 1.0])
```

安全状态逻辑按 `01-总体技术方案.md §五` 状态机规则：

| 条件 | 状态 |
|------|------|
| episode_length == 0 | `STANDBY` |
| wall_distance ≥ 0.001m | `SAFE_NAV` |
| 0.0005m ≤ wall_distance < 0.001m | `DANGER_WARNING` |
| wall_distance < 0.0005m | `COLLISION_STOP` |

#### 任务 D：`services/risk_assessor.py`

按 `03-API与通信协议.md §3.3` 规格实现多指标加权风险评估：

```python
class RiskAssessor:
    weights = {“wall_distance”: 0.4, “curvature”: 0.3, “velocity”: 0.2, “deviation”: 0.1}

    def assess(self, state: NavigationState) -> dict:
        “””返回综合风险分 [0, 1] 和各指标等级”””
```

#### 任务 E：WebSocket `state_update` 消息补全

在 `WebSocketHandler` 推送 `state_update` 时，将 `NavigationState.as_dict()` 中新字段透传，使消息结构与 `03-API与通信协议.md §1.4` 规格对齐。

#### 任务 F：测试覆盖

| 文件 | 新增测试 |
|------|---------|
| `tests/test_navigation_engine.py` | `TestPathProgressIntegration`：验证 path_progress 随 step 单调增 |
| `tests/test_navigation_engine.py` | `TestExtendedState`：验证新字段在 state 中存在且有合理值 |
| `tests/test_risk_assessor.py` | `TestRiskAssessor`：验证各指标边界条件和综合分计算 |

### 9.3 执行顺序

```
C（扩展 NavigationState 数据结构）
  └─ A（PathPlanner 注入 + path_progress / deviation）
  └─ B（wall_distance + curvature 计算）
  └─ D（RiskAssessor，依赖扩展后的 NavigationState）
  └─ E（WebSocket 透传，仅改消息序列化）
  └─ F（补充测试）
```

### 9.4 关键设计约束

- `NavigationEngine` 不得强依赖 `PathPlanner`：`planned_path` 可选，为 `None` 时不计算路径字段，与现有测试向下兼容
- `wall_distance` 初始实现可以用接触力代理（force > 0 时近似壁距 ≈ 0），后续再替换为精确几何查询
- `RiskAssessor` 只做无状态计算，不引入新的持久化依赖

### 9.5 完成标准

```
python -m pytest tests/test_navigation_engine.py tests/test_risk_assessor.py tests/test_websocket.py -v
```

预期：新增 ≥ 10 个测试全通过，已有 59 个测试无回归。

---

## 十、阶段七执行记录：风险评估 + 路径进度集成

### 10.1 当前结论

阶段七已完成。`NavigationState` 现已对齐 `03-API与通信协议.md §1.4` 的 `state_update` 字段规格，  
并新增独立的多指标风险评估器。所有新字段在 REST、WebSocket 两条链路上均已透传。

```text
NavigationEngine.step()
  -> wall_distance / curvature / tip_quaternion（MuJoCo physics 计算）
  -> path_progress / path_deviation（KDTree 最近点 + 弧长累积）
  -> safety_status（壁面距离状态机）
  -> NavigationState.as_dict()
  -> REST StepResponse / WebSocket state_update
RiskAssessor.assess(state)
  -> 4 指标加权风险分 + 分级
```

### 10.2 修改内容

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | `NavigationState` 新增 6 字段；`NavigationEngine` 新增 `planned_path` 参数与 `set_planned_path()`；新增 `_compute_wall_distance` / `_compute_curvature` / `_compute_path_progress` / `_compute_tip_quaternion` / `_compute_safety_status`；`get_safety_status` 改为返回 `state.safety_status` |
| `services/risk_assessor.py`（新建） | `RiskAssessor` 多指标加权风险评估，权重/阈值可配置 |
| `services/session_manager.py` | `create_session` 新增可选 `assets_dir` / `planned_path` 参数并透传 |
| `services/websocket_handler.py` | `_state_to_dict` 改为输出 `state.as_dict()`，补全全部新字段 |
| `services/schemas.py` | `NavigationStateResponse` 新增 6 字段 |
| `services/main.py` | `_state_to_response` 填充全部新字段 |

### 10.3 关键实现说明

1. **wall_distance（壁面距离代理）**
   - 从 `physics.data.contact.dist` 读取接触间隙，惩罚穿透（clip 到 0），无接触时返回哨兵值 `MAX_WALL_DISTANCE = 0.05m`
   - 已知局限：当前为「接触即贴壁」的粗粒度代理，DANGER 带较窄；后续可替换为精确几何最近点查询

2. **curvature（曲率）**
   - 基于最近 3 个尖端位置的 Menger 曲率（外接圆半径倒数），单位 m⁻¹
   - 历史不足 3 点、共线或重合时返回 0

3. **path_progress / path_deviation**
   - `planned_path` 为 MuJoCo 米制坐标点列；构建 `scipy.spatial.cKDTree`
   - progress = 最近路径顶点的累积弧长 / 总弧长，范围 [0, 1]
   - deviation = 到最近顶点欧氏距离（米）
   - `planned_path` 可选，为 `None` 时返回 (0, 0)，向下兼容

4. **tip_quaternion**
   - 从尖端 body 的 `physics.data.xquat` 读取，MuJoCo `[w,x,y,z]` 重排为协议约定的 `[x,y,z,w]`
   - 读取失败回退单位四元数

5. **safety_status 状态机**（按 `01-总体技术方案.md §五`）

   | 条件 | 状态 |
   |------|------|
   | episode_length == 0 | STANDBY |
   | wall_distance ≥ 0.001m | SAFE_NAV |
   | 0.0005m ≤ wall_distance < 0.001m | DANGER_WARNING |
   | wall_distance < 0.0005m | COLLISION_STOP |

### 10.4 新增测试

| 文件 | 新增内容 |
|------|---------|
| `tests/test_risk_assessor.py`（新建） | `TestRiskAssessorBands`（6）+ `TestRiskAssessorConfig`（2） |
| `tests/test_navigation_engine.py` | `TestNavigationStateExtended`（2）+ `TestNavigationEngineHelpers`（8，纯函数无需 MuJoCo）+ 2 个 VPP slow 集成测试 |

### 10.5 验收命令与结果

```powershell
python -m pytest tests/test_vhacd.py tests/test_navigation_engine.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_websocket.py tests/test_risk_assessor.py -q
```

实测结果：

```text
79 passed in 74.72s
```

较阶段六（59 passed）新增 20 个测试，已有测试无回归。

### 10.6 下一阶段建议

阶段八启动 Godot 客户端时，后端已可提供完整 `state_update`：  
`tip_position` / `tip_quaternion` / `wall_distance` / `curvature` / `path_progress` / `path_deviation` / `safety_status`，  
足以驱动 HUD 仪表盘与安全状态灯。

---

## 十一、阶段七.5 执行记录：WebSocket 协议补全

### 11.1 当前结论

阶段七.5 已完成。补齐了 `03-API与通信协议.md` 中三处与 Godot 客户端直接相关的协议缺口：  
`state_batch`（含导丝 body 渲染数据）、`path_request`/`path_response`（WebSocket 路径规划）、  
以及把阶段七建立的 `RiskAssessor` 真正接入状态流（`risk_score`）。

```text
control（batch_mode=true）
  -> NavigationEngine.step()
  -> get_render_bodies()（按 body 名含 "guidewire" 过滤 geom）
  -> state_batch { tip, bodies[], path, safety, episode }

path_request
  -> _get_path_planner(case_id)（lru_cache）
  -> PathPlanner.plan(A* + 可选 B-spline)
  -> path_response（PathResult.as_dict）

每步 _extract_state()
  -> RiskAssessor.assess(state)
  -> state.risk_score（进 state_update / state_batch）
```

### 11.2 修改内容

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | `NavigationState` 新增 `risk_score`；`NavigationEngine` 内置 `RiskAssessor` 并在 `_extract_state` 填充 `risk_score`；新增 `get_render_bodies()` 与 `planned_path` 属性 |
| `services/risk_assessor.py` | `NavigationState` 导入改为 `TYPE_CHECKING`，消除与 `navigation_engine` 的循环导入 |
| `services/websocket_handler.py` | `MessageType` 新增 `PATH_REQUEST`/`PATH_RESPONSE`；`SessionStartData` 新增 `batch_mode`；新增 `PathRequestData`；新增 `_handle_path_request` 与 `_state_to_batch`；`control`/`reset` 在 `batch_mode` 下发送 `state_batch`；模块级 `_get_path_planner`（lru_cache） |
| `services/session_manager.py` | `create_session` 透传 `assets_dir` / `planned_path`（阶段七已加） |

### 11.3 关键设计说明

1. **state_batch 触发方式**：采用会话级 `batch_mode` 开关（`session_start` 时指定），而非新增客户端消息类型。`batch_mode=false`（默认）时仍发 `state_update`，旧测试零回归；Godot 渲染导丝 Tube 时用 `batch_mode=true`。

2. **导丝 body 提取**：遍历 `model.ngeom`，按 `model.id2name(geom_bodyid, "body")` 含 `"guidewire"` 过滤，返回 `{pos, quat}`（四元数 `[w,x,y,z]`→`[x,y,z,w]`），自基座到尖端有序，供 Tube 渲染。

3. **path_request 无需会话**：客户端可在创建导航会话前先请求路径；`PathPlanner` 按 `case_id` 用 `lru_cache` 复用，坐标为 LPS 毫米。

4. **risk_score 与 safety_status 并存**：`safety_status` 仍由壁面距离状态机给出（安全状态灯），`risk_score`（0–1，四指标加权）用于 HUD 风险计。二者解耦，符合 doc §1.4 `safety.status` 与 `safety.risk_score` 分列的设计。

### 11.4 新增 / 修改测试

| 文件 | 内容 |
|------|------|
| `tests/test_websocket.py` | 新增 `TestWebSocketProtocolExtensions`（3 单元）+ `TestWebSocketPathRequest`（2，无需 MuJoCo）+ 2 个 slow 集成（`state_update` 扩展字段、`batch_mode` → `state_batch`） |

**附带修复**：`tests/test_websocket.py` 集成测试存在 ping 心跳竞态——5s ping 在慢速 MuJoCo 步骤中会插入消息流，导致 `receive_json` 偶发取到 `ping`。新增 `_recv()` 辅助函数跳过 ping 心跳，并替换全部 21 处 `receive_json` 调用，消除跨套件偶发失败。

### 11.5 验收命令与结果

```powershell
python -m pytest tests/test_vhacd.py tests/test_navigation_engine.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_websocket.py tests/test_risk_assessor.py -q
```

实测结果：

```text
86 passed in 110.34s
```

较阶段七（79 passed）新增 7 个测试，并修复 1 处跨套件 ping 竞态偶发失败。

### 11.6 协议对齐状态

| `03-API与通信协议.md` 消息 | 实现状态 |
|---------------------------|---------|
| `state_update`（含全部 §1.4 字段） | ✅ |
| `state_batch`（含 bodies 渲染数据） | ✅ |
| `path_request` / `path_response` | ✅ |
| `control` / `session_start` / `session_stop` / `reset` / `ping` / `pong` | ✅ |

后端 WebSocket 协议已与规格基本对齐，阶段八 Godot 客户端可直接对接。

---

## 十二、阶段八执行记录：Godot 客户端基础场景（首切片）

### 12.1 当前结论

阶段八已完成首个可运行切片。新建 Godot 4.4 客户端 `godot_client/`，
通过 WebSocket 对接后端，渲染血管 + 导丝，并支持键盘控制与 HUD。

```text
Godot 客户端
  -> websocket_client.gd 连接 /ws/session（batch_mode=true）
  -> 接收 state_batch { tip, bodies[84], path, safety, episode }
  -> guidewire_renderer 渲染尖端球 + 导丝 line strip
  -> hud_controller 显示安全灯 / 步数 / 速度 / 壁距 / 曲率 / 进度 / 风险
  -> input_handler WASD/R -> control/reset
```

### 12.2 新增内容

| 文件 | 作用 |
|------|------|
| `tools/export_godot_assets.py` | 从 `visual.stl`（MuJoCo 米制）导出 `blood_vessels.glb` 供 Godot 导入；可选抽稀 |
| `godot_client/project.godot` | Godot 4.4 项目配置（Forward+，后端 URL） |
| `godot_client/scenes/main.tscn` | 主场景（仅根节点 + 脚本，其余代码构建） |
| `godot_client/scripts/main_controller.gd` | 代码装配场景（环境/相机/光/血管/导丝/HUD/网络/输入）并连接信号 |
| `godot_client/scripts/websocket_client.gd` | WebSocket 协议客户端（session_start/control/reset/path_request/pong） |
| `godot_client/scripts/guidewire_renderer.gd` | 导丝尖端球 + 导丝体 line strip 渲染 |
| `godot_client/scripts/hud_controller.gd` | 安全状态灯 + 指标读数 HUD |
| `godot_client/scripts/input_handler.gd` | WASD/R 物理按键轮询 → 控制命令（~20Hz 限速） |
| `godot_client/README.md` | 运行/操作/协议对接/已知限制说明 |

### 12.3 关键设计说明

1. **血管资产管线**：Godot 无法读 VTK/STL，故用 `export_godot_assets.py` 从 `visual.stl`
   导出 GLB。`visual.stl` 已是 MuJoCo 米制，与导丝物理坐标同帧，二者天然对齐。

2. **最小 `.tscn` + 代码构建**：场景只挂根节点 + `main_controller.gd`，其余节点在
   `_ready()` 中构建，规避手写 `.tscn`/`InputMap` 序列化出错（无 Godot 编辑器可验证）。

3. **坐标对齐**：导丝渲染节点挂在血管 GLB 场景根之下，二者共享同一变换，
   抵消 Godot/trimesh glTF 轴转换，保证导丝叠加在血管内部对齐。

4. **batch_mode 渲染**：客户端以 `batch_mode=true` 开启会话，每步收 `state_batch`，
   含 84 节导丝 body 位置用于 line strip 渲染。

5. **键盘物理轮询**：用 `Input.is_physical_key_pressed`，不依赖自定义 InputMap。

### 12.4 验收

本阶段产物为 Godot 工程（GDScript + `.tscn` + GLB），当前环境无 Godot 编辑器，
无法运行编辑器自动化验证。已完成以下可验证项：

1. **GLB 资产生成与校验**：

   ```text
   blood_vessels.glb: 312507 verts / 625124 faces
   bounds 与 visual.stl 一致（同帧），10.73 MB
   ```

2. **客户端—后端字段契约回放**（用后端 `TestClient` 模拟客户端精确消息流）：

   ```text
   session_started OK（含顶层 session_id 与 data.state）
   state_batch OK: bodies 84, tip.position, safety.status=SAFE_NAV,
                   safety.{risk_score,speed,wall_distance,curvature}, path.progress, episode.length=1
   path_response OK: node_count 1261
   CLIENT-CONTRACT SMOKE PASSED
   ```

   该回放确认 `websocket_client.gd` / `main_controller.gd` 读取的每个字段名后端均产出。

3. **后端回归**：阶段八仅新增文件，未改 `services/`，全量测试无回归：

   ```text
   86 passed
   ```

### 12.5 已知限制

- 坐标对齐依赖「导丝挂血管根下」假设；若实测偏移需对齐到血管 MeshInstance3D 变换。
- 后端冷启动 MuJoCo 初始化会同步阻塞事件循环数秒；客户端每帧回 `pong` 可维持连接，
  但单步阻塞超 15s 仍可能 `PONG_TIMEOUT`（后端可改线程池执行 step 根治）。
- 本切片未含 X-ray 着色器、双视角、手柄映射、路径线可视化。

### 12.6 下一阶段建议

- **阶段九**：后端 step 改用 `run_in_executor` 线程池，避免阻塞事件循环（根治 pong 超时与卡顿）。
- **阶段十**：Godot 路径可视化（`path_response` → Path3D 折线）+ X-ray 着色器 + 手柄映射。

---

## 十三、阶段九执行记录：实机联调与根治

### 13.1 当前结论

在 Godot 4.6 实机联调中暴露并**逐项根治**了一连串问题，最终实现：
导丝在可见的 low_tort 体模内**流畅推进/后退/转向**，连接稳定、控制无积压延迟。

```text
Godot 客户端（lock-step 控制）
  -> 后端线程池执行 MuJoCo step（事件循环不阻塞、心跳稳定）
  -> 交互档 n_bodies=40 / n_substeps=2（~30 步/秒）
  -> state_batch 渲染导丝（叠加在 low_tort 体模内，坐标对齐）
```

### 13.2 问题链与根因（按定位顺序）

| 现象 | 根因 | 修复 |
|------|------|------|
| 打开 Godot 全黑 | 相机在入树前设 `current=true` 未生效 | `add_child` 后调 `make_current()` |
| 连上但所有数据为 0 | 会话建不起来，控制刷屏 `NO_SESSION` 覆盖真错误 | 客户端会话建好前不发控制；`session_start` 每 3s 重试自愈首包丢失 |
| `PARSE_ERROR`、后端无 traceback | `_handle_session_start` 只 `except RuntimeError`，其它异常漏成误导性 `PARSE_ERROR` | 放宽为 `except Exception` + `traceback.print_exc()`，错误带类型文字 |
| `ModuleNotFoundError: No module named 'cathsim'` | uvicorn 跑在没装 cathsim 的环境 | navigation_engine 顶部加 `src` 路径 bootstrap 兜底 |
| `.venv` 跑 uvicorn 报 `No module named uvicorn` | `.venv` 装了 fastapi（TestClient 够用）但没装 uvicorn | `pip install uvicorn[standard]` 进 `.venv` |
| 连上跑几十条后 `DISCONNECTED` | MuJoCo `step` 同步阻塞 async 事件循环 → 心跳超时 | 阻塞调用全部 `run_in_executor` 线程池化；`PONG_TIMEOUT` 15s→45s |
| 控制延迟高、滞后才反应 | 客户端 20Hz 发控制 > 后端步进吞吐 → 积压 | 客户端 **lock-step**：一次一条、收回执再发，配速到后端吞吐 |
| 单步 359ms、仅 ~3 步/秒 | 80 节导丝 + 235 凸包，约束求解器接触量大（`mj_step2` 129ms/子步） | `n_bodies`/`n_substeps` 可配置，交互档 40/2 → 28ms/步、~35 步/秒 |
| 导丝到不了血管、推进一半停 | **仿真用 low_tort 体模，渲染却是 VPP 大血管**，两者异坐标异尺度 | 渲染改为被仿真的 low_tort 体模（scale=1、原点），导丝叠加对齐 |

### 13.3 修改内容

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | 顶部 `src` 路径 bootstrap；`NavigationEngine`/`make_dm_env` 调用新增 `n_bodies`/`n_substeps`；`get_render_bodies` 缓存导丝几何 id |
| `src/cathsim/dm/env.py` | `make_dm_env` 新增 `n_bodies=80`、`n_substeps=None` 参数（默认保持原行为）；`n_substeps` 经 `task.control_timestep` 覆盖 |
| `services/session_manager.py` | `create_session` 透传 `n_bodies`/`n_substeps` |
| `services/websocket_handler.py` | 新增 `_run_blocking`（线程池）；`create_session`/`step`/`reset`/`plan` 全部线程池化；`SessionStartData` 增 `n_bodies`/`n_substeps`；会话异常放宽为 `except Exception` + traceback；`PONG_TIMEOUT` 45s |
| `tools/export_godot_assets.py` | 新增 `--phantom` 模式，导出 CathSim 体模视觉网格为 GLB |
| `godot_client/scripts/websocket_client.gd` | `session_start` 重试；lock-step 控制（在途一条 + `ack_timeout` 兜底）；交互档 `n_bodies=40`/`n_substeps=2`；忽略良性 `SESSION_EXISTS` |
| `godot_client/scripts/main_controller.gd` | 相机 `make_current()`；渲染 `low_tort.glb`；`session_started` 应用初始状态；屏幕诊断（session/消息计数/错误） |
| `godot_client/scripts/hud_controller.gd` | 重做 HUD：状态灯 + 指标 + 实时输入读数 + 屏幕诊断行 |
| `godot_client/scripts/input_handler.gd` | 新增 `input_state`（每帧，供 HUD）信号，与 `control`（仅非零，发后端）分离 |

### 13.4 性能实测（low_tort，本机）

| 配置 | 单步耗时 | 吞吐 |
|------|---------|------|
| n=80 sub=3（全保真，默认） | 359 ms | ~2.8/s |
| n=40 sub=2（交互档，客户端默认） | 28 ms | **~35/s** |
| n=40 sub=1 | 23 ms | ~43/s |

> 冷启动 reset ~17–23s（235 凸包模型编译，一次性）；线程池 + `PONG_TIMEOUT=45s` 保证此间不掉线。
> 瓶颈为 MuJoCo 约束求解器 `mj_step2`，与接触量（导丝节数）和子步数线性相关。

### 13.5 坐标对齐方案

- 渲染**被仿真的体模**：`low_tort/visual.stl`（8568 顶点、原点、scale=1）导出为 `low_tort.glb`。
- 导丝渲染节点挂在体模 GLB 场景根之下，二者共享 glTF 轴变换 → 导丝叠加在血管内部对齐。
- VPP 真实血管导航（导丝起点对齐到 VPP 入口）留作阶段十。

### 13.6 验收

- 维护测试套件（`navigation_engine`/`websocket`/`risk_assessor`/`services_api` 等）**73 passed**；
  `test_task.py::test_camera_matrix` 的 3 个失败经 `git stash` 验证为**改动前即存在**（与本阶段无关）。
- 实机：导丝在可见 low_tort 体模内流畅推进/后退/转向，连接稳定无积压延迟（用户确认）。

### 13.7 启动方式（重要）

后端必须用装齐依赖的 `.venv` Python 启动：

```powershell
.\.venv\Scripts\python.exe -m uvicorn services.main:app --host 0.0.0.0 --port 8000
```

体模 GLB 生成（首次或换 phantom 时）：

```powershell
python tools/export_godot_assets.py --phantom low_tort
```

### 13.8 下一阶段建议

- **阶段十**：VPP 真实血管导航（导丝起点对齐 VPP 入口 + case_001_vpp 导航调试）；
  路径可视化（`path_response` → Path3D）；X-ray 着色器；手柄映射；按 phantom 自动选渲染 GLB。

---

## 十四、阶段十执行记录：VPP 真实血管导航（任务 A 入口对齐 / B 按 phantom 选 GLB / C 路径可视化）

### 14.1 当前结论

阶段十任务 A/B/C 已完成（§14.2–14.7 为任务 A，§14.8 为任务 B/C）。根治了阶段九 §13.5
遗留的"VPP 真实血管导航：导丝起点对齐 VPP 入口"，并打通 Godot 客户端 VPP 端到端接入
（按 phantom 选渲染 GLB + 规划路径折线可视化）。
导丝现可在偏离世界原点约 1m 的 VPP 血管（`case_001_vpp`，235 凸包）**入口处生成、贴合中心线并沿血管推进**。

```text
PathPlanner.plan(start, end)           # LPS mm
  -> waypoints / smooth_waypoints
  -> ÷1000 转 MuJoCo 米制 -> planned_path
  -> NavigationEngine(planned_path=...)
       _resolve_entry(): entry = path[0], dir = 首个非退化路段
  -> make_dm_env(entry_point, entry_direction)
  -> Navigate.initialize_episode():
       导丝 set_pose(position=entry+jitter, quaternion=对齐 dir)
  -> 导丝在血管入口生成、朝向血管走向、推进进入血管
```

### 14.2 根因与修复

| 现象 | 根因 | 修复 |
|------|------|------|
| 导丝到不了 VPP 血管 | `initialize_episode` 把导丝生成在世界原点附近（`UniformSphere`），而 VPP 血管经 LPS mm÷1000 平移后整体离原点约 1m | 新增 `entry_point` 让导丝在血管入口生成 |
| 即便平移到入口也顶墙 | 导丝默认进给轴为世界 +y，与 VPP 血管入口走向不一致 | 新增 `entry_direction` + `quat_from_vectors` 把进给轴旋转对齐血管走向 |

**坐标一致性**：V-HACD 对网格仅做 ÷1000（无轴变换），故 graph 节点 / endpoint site / 规划路径
（均为 LPS mm）÷1000 后与 phantom 网格、site 同帧，路径点可直接作为 MuJoCo 米制入口与进度参考。

### 14.3 修改内容

| 文件 | 改动 |
|------|------|
| `src/cathsim/dm/env.py` | 新增模块级 `GUIDEWIRE_FEED_AXIS=[0,1,0]`（经 low_tort 实测校准）与 `quat_from_vectors()`；`Navigate.__init__` 新增 `entry_point`/`entry_direction`，预算入口四元数；`initialize_episode` 按入口点+jitter+朝向设置导丝位姿；`make_dm_env` 透传两参数 |
| `services/navigation_engine.py` | `NavigationEngine.__init__` 新增 `entry_point`/`entry_direction`；新增 `_resolve_entry()`——显式入口优先，否则从 `planned_path` 派生（入口=path[0]，方向=首个非退化路段）；`_ensure_initialized` 透传入口 |

`session_manager.create_session` 已透传 `planned_path`（阶段七加入），故入口派生对 REST/WebSocket
链路自动生效，无需新增会话参数：客户端只要在 `session_start` 带上 `planned_path`（米制），
导丝即在 VPP 入口生成。

### 14.4 关键实现说明

1. **进给轴校准**：low_tort 默认体模下，正向 push 25 步实测尖端位移方向 `[0.072, 0.997, -0.027] ≈ +y`，
   故 `GUIDEWIRE_FEED_AXIS=[0,1,0]`。入口对齐即把该轴旋转到 `entry_direction`。

2. **入口四元数**：`quat_from_vectors(a, b)` 返回把 a 旋到 b 的 MuJoCo `[w,x,y,z]` 四元数，
   处理平行 / 反平行退化；`set_pose` 写附着帧 `pos`/`quat`（导丝无 freejoint，运行时改附着帧生效）。

3. **向下兼容**：`entry_point=None` 时 `initialize_episode` 行为与改动前完全一致（low_tort 集成测试 5 passed 回归确认）。

### 14.5 端到端 smoke 实测（case_001，endpoints_24 → endpoints_1）

```text
path nodes 2152  len_mm 1135.1
entry(m)  [-0.0015 -0.2685  0.2904]  -> goal(m) [-0.9953 -0.2202  0.2479]
resolved entry [-0.0015 -0.2685 0.2904]  dir [-0.956 -0.005 0.292]
reset  tip [-0.019 -0.2692 0.2957]  deviation 0.0026   # 起点贴合中心线 (2.6mm)
50 steps push=0.8:
  progress 0.043  contact_force 4.18  wall 0.0026  safety SAFE_NAV
```

导丝在入口处生成（reset 偏离 2.6mm，改动前离血管约 1m），推进时血管接触正常、progress 递增。
开环纯推进下 deviation 渐增（50 步约 27mm）属转向控制范畴（操作者用 rotate 转向），非入口缺陷。

### 14.6 新增测试

| 文件 | 新增内容 |
|------|---------|
| `tests/test_navigation_engine.py` | `TestNavigationEngineHelpers` 新增 4 个 `_resolve_entry` 纯函数测试；新增 `TestQuatFromVectors`（3，slow）；`TestVPPPhantomIntegration` 新增 `test_vpp_guidewire_spawns_at_planned_path_entry`（slow，验证 reset 偏离 < 20mm 且尖端远离原点） |

### 14.7 验收命令与结果

```powershell
$env:MUJOCO_GL="glfw"
.\.venv\Scripts\python.exe -m pytest tests/test_navigation_engine.py tests/test_risk_assessor.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_vhacd.py tests/test_websocket.py -q
```

实测结果：**94 passed**（较阶段九维护套件 73 新增覆盖；新增 8 个测试，已有测试无回归）。

### 14.8 任务 B/C 执行记录：VPP 客户端接入 + 路径可视化

在任务 A（导丝入口对齐）基础上，完成 VPP 真实血管的端到端客户端接入。

#### 14.8.1 后端 VPP-over-WebSocket 启用（任务 B/C 前置）

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | 新增 `resolve_vpp_assets_dir(phantom)`：`<case>_vpp` → `data/vpp_assets/<case>/mujoco`；`NavigationEngine` 在 `assets_dir=None` 时按 phantom 名自动解析。客户端只传 phantom 名即可加载 VPP phantom，无需暴露服务器路径 |
| `services/websocket_handler.py` | `SessionStartData` 新增 `case_id`/`start_position`/`end_position`/`smooth`/`planned_path`；新增 `_resolve_session_path()`——显式 `planned_path` 优先，否则按 `start/end`（LPS mm）服务端规划 A*+B-spline 并转米制；`create_session` 透传 `planned_path` → 自动入口对齐 |

设计取舍：把 VPP 导航装配集中到服务端，客户端**一条 `session_start`**（phantom + start/end endpoint）即可启动；
后端完成"规划路径 → 转米制 → 入口对齐 → 路径回传"，`state_batch.path.waypoints` 同帧供渲染。

#### 14.8.2 任务 B：Godot 按 phantom 自动选渲染 GLB

| 文件 | 改动 |
|------|------|
| `godot_client/scripts/main_controller.gd` | 移除硬编码 `low_tort.glb`；新增导出变量 `phantom`/`target`/`case_id`/`start_position`/`end_position`（单一配置源，推给 ws 节点）；`_resolve_vessel_glb()` 按 phantom 选 `res://assets/models/<phantom>.glb`，`*_vpp` 回退 `blood_vessels.glb`，否则 `low_tort.glb` |
| `godot_client/scripts/websocket_client.gd` | 新增 `case_id`/`start_position`/`end_position`/`smooth` 导出；`_build_session_start()` 仅在配置了 VPP 路由（start/end 各 3 维）时附带规划字段，low_tort 会话保持原样 |

#### 14.8.3 任务 C：Godot 路径可视化

| 文件 | 改动 |
|------|------|
| `godot_client/scripts/path_renderer.gd`（新建） | 渲染 `state_batch.path.waypoints` 为蓝色 line strip；挂在血管 GLB 根下与导丝同帧；按 waypoint 数变化才重绘（路径会话内恒定） |
| `godot_client/scripts/main_controller.gd` | `_setup_path()` 装配 path_renderer；`_on_batch` 调 `_path.update_from_batch(batch)` |

#### 14.8.4 新增测试

| 文件 | 内容 |
|------|------|
| `tests/test_websocket.py` | `TestSessionStartDataVPP`（2 单元：VPP 字段默认值、`resolve_vpp_assets_dir`）；`TestWebSocketVPPNavigation`（1 slow：`session_start` 服务端规划 → reset 偏离 < 20mm + `state_batch` 含 path.waypoints/bodies） |

#### 14.8.5 验收

后端契约回放（TestClient，模拟 Godot 精确消息流）：

```text
session_start(phantom=case_001_vpp, start/end endpoint LPS mm)
  -> session_started: path_deviation 0.0021   # 服务端规划 + 入口对齐
control -> state_batch: bodies 44, path.waypoints 2152, progress 递增
```

测试结果（维护套件）：

```powershell
$env:MUJOCO_GL="glfw"
.\.venv\Scripts\python.exe -m pytest tests/test_websocket.py tests/test_navigation_engine.py tests/test_risk_assessor.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_vhacd.py -q
```

`test_websocket.py + test_navigation_engine.py` 子集 **68 passed**（含全部 slow）；
连同 risk/api/vpp_path/vhacd 全维护套件 **97 passed**，无回归。

Godot 工程为 GDScript + GLB，当前环境无 Godot 编辑器，仅能契约回放间接验证（同阶段八做法）。

### 14.9 下一阶段建议

- **任务 D/E**：X-ray 着色器、双视角、手柄映射（独立，可并行）。
- 实机：用 `.venv` 启后端，Godot 设 `phantom=case_001_vpp` + start/end endpoint，
  实测导丝在 VPP 血管内导航 + 路径折线对齐（需 Godot 编辑器环境）。
- `wall_distance` 仍为接触代理（COLLISION_STOP 带偏窄）；可替换为精确几何最近点查询。

---

## 十五、阶段十一执行记录：近距观察相机系统

### 15.1 当前结论

阶段十一完成 Godot 客户端的**多模式相机系统**，根治了"概览视角离 VPP 血管太远、
导丝缩成一个点"的观察问题，并新增第一人称血管内窥镜视角。

```text
state_batch.tip { position, direction, quaternion }   # 后端已有，零后端改动
  -> CameraRig.update_tip()（挂血管 GLB 根下，tip 为本地坐标，无需换算）
  -> FOLLOW    第三人称近距跟随（位置 lerp + look 平滑）
  -> ENDOSCOPE 第一人称内窥镜（尖端处沿 direction 看血管前方，quaternion 定滚转）
  -> C 键循环 OVERVIEW / FOLLOW / ENDOSCOPE
```

### 15.2 设计要点

1. **零后端 / 零协议改动**：第三人称与第一人称所需的 `tip.position` / `tip.direction`
   / `tip.quaternion` 在阶段七已进 `state_batch`，客户端直接复用。

2. **坐标对齐**：`CameraRig` 与导丝 / 路径渲染同挂血管 GLB 根 `frame` 之下，
   tip 坐标作为**帧本地坐标**直接使用，自动抵消 glTF 轴变换，与现有渲染对齐策略一致。

3. **三模式职责分离**：
   - `OVERVIEW`：保留原整血管 AABB 框定（main_controller 拥有，全局空间）。
   - `FOLLOW`：相机位 = `tip − dir·follow_back + UP·follow_height`，`look_at(tip)`；
     位置 lerp + look 目标 lerp 平滑，消除每步跳变抖动；`back≈2.5cm`、`height≈1.2cm`
     使导丝充满画面（解决"缩成一点"）。
   - `ENDOSCOPE`：相机位 = `tip − dir·2mm`（略后退避免穿模），沿 `direction` 看血管前方，
     up 由 `quaternion` 派生（支持滚转），`near=0.5mm` 置身管腔内。

4. **退化保护**：`update_tip` 忽略零向量 `direction` / 零四元数（STANDBY 期保留上一帧朝向）；
   `_safe_up()` 保证 look_at 的 up 向量不与前向平行。

5. **自动切入**：首帧 tip 到达后自动从 OVERVIEW 切到 FOLLOW，操作者无需手动即看到导丝近景；
   之后 `C` 键循环三模式，HUD 显示当前视角名。

### 15.3 修改内容

| 文件 | 改动 |
|------|------|
| `godot_client/scripts/camera_rig.gd`（新建） | `CameraRig`：follow / endoscope 两台相机，`update_tip()`，平滑跟随，退化保护，参数可 `@export` 调 |
| `godot_client/scripts/main_controller.gd` | 新增 `CamMode` 枚举与 `_setup_rig()`（挂 frame 下）；`_on_batch` 调 `_feed_rig(tip)`；`_cycle_camera_mode` / `_set_camera_mode`（make_current 切换）；首帧自动切 FOLLOW；`_to_vec3` / `_to_quat` 辅助 |
| `godot_client/scripts/input_handler.gd` | 新增 `view_cycle` 信号，`C` 键边沿触发 |
| `godot_client/scripts/hud_controller.gd` | 新增视角模式行 `set_view_mode()`；控制提示补 `C 切换视角` |

### 15.4 验收

纯客户端渲染，本环境无 Godot 编辑器，无法运行编辑器自动化验证（同阶段八/十做法）。
已完成离线核查：四个脚本 GDScript API / 信号连线 / 坐标帧一致性自查。

**实机验证待办**（需 Godot 编辑器）：

- 启后端（`.venv` uvicorn），Godot 运行后导丝应自动进入 FOLLOW 近景，导丝充满画面（非一点）。
- `C` 键循环：FOLLOW → ENDOSCOPE → OVERVIEW，HUD 视角行同步。
- ENDOSCOPE 下沿血管推进时的管腔观感与跟手平滑度（`follow_smooth` / `*_back` / `fov` 可调）。

### 15.5 已知限制 / 下一步

- ENDOSCOPE 下血管材质为半透明（alpha 0.28 + CULL_DISABLED），从内部看会"穿透"；
  如需真实管腔观感，可为内窥镜模式切换不透明内壁 / 渲背面材质（增量微调，未实现）。
- 相机平滑 / 距离 / FOV 参数已 `@export`，实机手感由操作者调定。
- 仍待做（与本阶段独立）：X-ray 荧光透视着色器、双视角分屏、Xbox/PS5 手柄映射（doc/01 §路线图）。

---

## 十六、阶段十二执行记录：血管入口高亮（介入血管入路可视化）

### 16.1 背景与诊断

实机现象：多视角已可用，但操作者反映"导丝始终在血管外面无法进入"。

诊断结论——**后端入口对齐本身是好的**（阶段十已实现）：

```text
PathPlanner.plan(start, end)        # LPS mm
  -> ÷1000 -> planned_path（米制）
  -> NavigationEngine._resolve_entry(): entry = path[0]，方向 = 首个非退化路段
  -> Navigate.initialize_episode(): 导丝在 entry+jitter 生成、quat 对齐进给方向
```

阶段十 smoke 实测 reset 偏离中心线仅 **2.6mm**，即导丝物理上**确实生成在血管入口内**。
真正的问题是**可见性**：VPP 血管离世界原点约 1m，概览视角下导丝缩成一点，血管半透明，
操作者无从判断入口在哪、导丝是否在管腔内。按介入手术"血管入路点"语义，需要把入口**显式高亮**。

### 16.2 修改内容

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | 新增 `entry_pose` 属性：返回 `_resolve_entry()` 解析出的入口 `{position, direction}`（米制、方向归一化），即导丝真实生成位姿；无入口时返回空列表（向下兼容 low_tort） |
| `services/websocket_handler.py` | `_state_to_batch` 新增 `entry`（随首帧/reset 下发，会话内恒定）与 `target`（每帧）标记块 |
| `godot_client/scripts/entry_marker.gd`（新建） | 渲染**入口**（亮绿自发光球 + 沿进给方向箭头）与**目标**（红色自发光球）；挂血管 GLB 根下与导丝/路径同帧，无需坐标换算；`entry` 缓存（空帧保留上次绘制） |
| `godot_client/scripts/main_controller.gd` | `_setup_entry_marker()` 装配；`_on_batch` 调 `update_from_batch(batch)` |
| `godot_client/scripts/hud_controller.gd` | 新增颜色图例行：● 入口 Entry（绿）/ ● 目标 Target（红）/ ● 路径 Path（蓝） |

### 16.3 关键设计说明

1. **入口 = 介入血管入路点 = path[0]**：与路径规划起点同一点，方向为首个非退化路段（即沿中心线导入方向），符合"导丝在入路处沿血管走向送入"的介入语义。
2. **后端给真值，客户端只渲染**：`entry_pose` 直接来自 `_resolve_entry()`，与导丝 reset 生成点是**同一来源**，故绿色标记必然落在导丝实际生成处——既高亮入口，也能直观核查导丝是否对齐血管。
3. **颜色约定**：避开现有配色（血管半透红、路径蓝、导丝白、尖端橙），入口取**亮绿**（go/入路）、目标取**红**，对比鲜明。
4. **向下兼容**：low_tort 无规划路径时 `entry_pose` 返回空、`entry` 块为空字典，客户端不绘制入口标记，零回归。

### 16.4 新增 / 修改测试

| 文件 | 内容 |
|------|------|
| `tests/test_navigation_engine.py` | `TestNavigationEngineHelpers` 新增 `test_entry_pose_empty_without_path`、`test_entry_pose_from_planned_path_is_normalized`（纯函数，无需 MuJoCo） |
| `tests/test_websocket.py` | `test_vpp_session_plans_path_and_aligns_entry` 增补断言：`state_batch.entry.position/direction` 各 3 维、`entry.position == path.waypoints[0]`、`target` 3 维 |

### 16.5 验收命令与结果

```powershell
$env:MUJOCO_GL="glfw"
.\.venv\Scripts\python.exe -m pytest tests/test_websocket.py tests/test_navigation_engine.py -q
```

实测结果：**70 passed**（含全部 slow），新增 2 个纯函数测试 + 1 处 VPP 集成断言增补，已有测试无回归。
VPP 集成测试确认 `entry.position` 与 `path.waypoints[0]` 完全一致（abs < 1e-6），即绿色入口标记落在导丝真实生成点。

Godot 工程为 GDScript + GLB，本环境无 Godot 编辑器，仅离线核查脚本 API / 信号连线 / 坐标帧一致性（同阶段八/十/十一做法）。

### 16.6 实机验证待办（需 Godot 编辑器）

- 启后端（`.venv` uvicorn），Godot 设 `phantom=case_001_vpp` + start/end endpoint 运行。
- 概览/跟随视角下应见**亮绿球 + 箭头**标出血管入口，**红球**标出目标，导丝尖端应从绿色入口处沿路径推进。

> 注（阶段十三补充）：实机加入入口标记后发现导丝能到入口附近却"无论怎么移动都到不了目标"。
> 排查证明**不是渲染对齐问题**（两个 GLB 均为单位变换 + 原始物理坐标，导丝/入口/血管同帧），
> 而是**几何/物理尺度不匹配**——见 §十七。

---

## 十七、阶段十三执行记录：VPP 全程导航——运动学循路模式（guided）

### 17.1 根因定位（推翻"渲染对齐"假设）

阶段十二加入绿色入口标记后，实机现象变为"导丝能到入口附近、但无论推/转都到不了目标"。
逐项排查，用客户端真实参数（`case_001_vpp`，n_bodies=40，start=Endpoints-24→end=Endpoints-1）跑后端实测：

| 项 | 值 | 结论 |
|----|----|------|
| 两 GLB 节点变换 | 单位阵；顶点为原始物理坐标 | **排除轴向/渲染错位**；导丝/入口/血管同帧 |
| 导丝长度（n_bodies=40） | **82 mm** | 远短于血管 |
| slider 插入行程硬上限 | **200 mm**（`range=[0,0.2]`）| 远短于路径 |
| VPP 规划路径长度 | **1136 mm** | — |
| 纯推进实测 | 第一个弯**屈曲顶墙**，slider 卡在 ~36mm、进度 4.3%、接触力饱和 5N | 物理上**到不了** |

**根因**：CathSim 导丝模型是为 ~150mm 小主动脉体模（low_tort/phantom3）设计的；放进完整 VPP 血管
（路径 1.1m），"82mm 导丝 + 200mm 行程"既会在首个弯屈曲卡死，几何上也根本够不着远端目标。
low_tort 实机正常正是因为它在原点附近且尺度匹配（阶段九已验证），而 VPP 全程导航**从未实机验证过**（§14.9 为 TODO）。

### 17.2 解决方案：运动学循路模式（guided）

经确认（用户选择"完整真实导丝 + 自动循路"），鉴于在 235 凸包血管内模拟 ~700 节长导丝在交互帧率下不可行，
采用**后端运动学循路**：导丝沿规划中心线由"插入深度"参数驱动，绕过 MuJoCo 物理，保证从入口一路可达目标。

```text
guided + planned_path
  -> reset(): s=0，tip 落在 path[0]（入口）
  -> step(push): s = clip(s + push·advance_per_step, 0, total_len)
  -> tip = 路径弧长 s 处插值；direction = 该处切线
  -> get_render_bodies(): 沿 [s-wire_length, s] 采样中心线为导丝体
  -> path_progress = s/total_len（必达 1.0）；deviation = 0
```

物理模式（low_tort 等短体模、无 planned_path）完全保持不变，向下兼容。

### 17.3 修改内容

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | `NavigationEngine` 新增 `guided`/`advance_per_step`/`wire_length` 参数与 `_s` 插入深度；新增 `_is_guided`/`_point_at_arclen`/`_tangent_at_arclen`/`_quat_from_direction`/`_guided_state`/`_guided_render_bodies`；`reset`/`step`/`get_render_bodies` 增加 guided 分支；`_compute_velocity` 兼容无 MuJoCo 环境 |
| `services/session_manager.py` | `create_session` 新增 `guided` 透传 |
| `services/websocket_handler.py` | `SessionStartData` 新增 `guided`；`_handle_session_start` 对 `*_vpp` + 已规划路径**自动启用** guided |
| `godot_client/scripts/websocket_client.gd` | 新增 `@export guided=true`，`session_start` 带 `guided`（仅在有路径时生效，low_tort 不受影响）|

### 17.4 关键设计说明

1. **自动启用**：`guided = params.guided or (planned_path and phantom 以 _vpp 结尾)`——VPP 路由开箱即用，客户端无需额外配置；low_tort 无路径时 `_is_guided()` 恒 False，仍走物理。
2. **绿色入口标记即真值**：guided 下 reset tip 精确落在 `path[0]`，与阶段十二的入口标记完全重合（实测 dist=0）。
3. **可正可负**：`push>0` 进、`push<0` 退；`s` 夹在 `[0, total_len]`，progress 到 1.0 即 `done=True`，尖端停在目标。
4. **无 MuJoCo 开销**：guided 会话不加载 235 凸包 phantom，无 ~17s 冷启动、无 28ms/步物理；瞬时、确定、必达。
5. **状态完整**：guided 合成的 `NavigationState` 含 tip/direction/quaternion/curvature/progress/risk 等全字段，REST/WebSocket/state_batch 链路与渲染零改动复用。

### 17.5 端到端实测（case_001，Endpoints-24 → Endpoints-1）

```text
path len 1.136m / 2152 nodes
GUIDED reset: tip=[-0.0015 -0.2685 0.2904] == entry（dist 0.0）, prog 0, STANDBY, bodies 25
push=1.0：prog 0.264 -> 0.528 -> 0.792 -> 1.000（step 76），tip 精确到达 goal（dist2goal 0.0），done=True
retract：prog 回退（1.0 -> 0.934）
```

导丝从入口沿中心线一路推进至目标、可前可后，彻底解决"无论怎么移动都到不了"。

### 17.6 新增 / 修改测试

| 文件 | 内容 |
|------|------|
| `tests/test_navigation_engine.py` | 新增 `TestGuidedMode`（5，纯函数无需 MuJoCo）：需路径才生效、reset 落在入口、推进单调到达目标、回退减进度、导丝体贴合路径 |
| `tests/test_websocket.py` | `test_vpp_session_plans_path_and_aligns_entry` 增补：guided 自动启用下连续推进 progress 递增（限速配速避免丢控制）；该测试改由 guided 跑、耗时 17s→4s |

### 17.7 验收命令与结果

```powershell
$env:MUJOCO_GL="glfw"
.\.venv\Scripts\python.exe -m pytest tests/test_websocket.py tests/test_navigation_engine.py tests/test_risk_assessor.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_vhacd.py -q
```

实测结果：**104 passed**（较阶段十二 97 新增 7：5 guided + 2 entry_pose），已有测试无回归。

### 17.8 已知限制 / 下一步

- guided 为运动学循路，**不建模接触力**（`wall_distance` 为自由空间哨兵、`contact_force=0`）；
  适合 VPP 全程导航演示与路径跟随，不替代物理碰撞训练。如需物理真实度，应另做长导丝 + 解算优化（性能代价大）。
- `advance_per_step`（默认 10mm/单位推进）决定循路速度，可按手感调；`rotate` 在 guided 下暂未用于滚转。
- 可选增强：guided 下让相机/HUD 显式标注"循路模式"，与物理模式区分。

---

## 十八、阶段十四：导丝物理化外观（方案评审与决策）

### 18.1 问题反馈（实机）

guided 循路模式虽能到达目标，但物理形态缺失：

1. 跟随模式下导丝呈**锯齿状**——guided 只发抽象路径点、且客户端渲染为无粗细细线，且只显示尖端最后 12cm。
2. 只按规划路径**简单移动**，忽略真实物理：导丝进入血管会弯曲、会碰壁，转弯处看不到导丝弯曲曲折。

### 18.2 根本张力

在完整 1.1m VPP 血管里跑**真实 MuJoCo 物理本质上很难**：CathSim 导丝模型为 ~150mm 小体模设计、
slider 直线进给在第一个弯（~36mm 处）就屈曲卡死；要走完 1.1m 需数百节导丝 → 交互帧率崩溃，且不保证调得通。
故"完全真实物理"与"可靠流畅"在当前模型下不可兼得。

### 18.3 三方案

| 方案 | 做法 | 真实度 | 可靠/性能 | 风险 |
|------|------|--------|-----------|------|
| A 循路+物理化外观 | guided 可靠到达 + ①整条已插入导丝渲染成平滑管状(消锯齿) ②全长显示随每个弯曲折 ③转弯处贴向内壁 | 视觉真实(弯曲贴壁随血管曲折) | 高、流畅 | 低 |
| B 真实物理导丝 | 加长导丝+延长行程+中心线吸引力辅助，MuJoCo 真实弯曲/碰壁 | 物理真实(含接触力) | 帧率大降、可能走不完整 | 高、不保证 |
| C 分场景双模式 | 短体模物理训练 / 长 VPP 用 A，HUD 标注可切换 | 按场景 | 中 | 中 |

### 18.4 决策

采用 **方案 A（循路 + 物理化外观）**（用户确认）。在 §十九 记录实现。
方案 B/C 作为后续可选增强保留：若后续需要真实接触力训练，再单独立项做长导丝 + 解算优化与双模式切换。

---

## 十九、阶段十四执行记录：导丝物理化外观（方案 A）

### 19.1 当前结论

在 guided 循路基础上，让导丝在视觉上成为一根**平滑、弯曲、贴内壁、随血管全长曲折**的真实导丝，
同时保持循路的可靠到达与流畅帧率。三处改动对应三个反馈点：

```text
后端 _guided_render_bodies()
  -> 采样整条已插入导丝（entry s=0 -> tip s）        # 全长显示，随每个弯曲折
  -> 每点按局部曲率向内侧偏移（_inner_wall_offset）   # 转弯贴内壁
客户端 guidewire_renderer.gd
  -> 平行传输坐标架挤出圆环管(ImmediateMesh 三角面)   # 消锯齿、有粗细
  -> 受光金属材质                                     # 曲率经明暗可见
```

### 19.2 修改内容

| 文件 | 改动 |
|------|------|
| `services/navigation_engine.py` | `_guided_render_bodies` 改为采样 **entry→tip 全长**（~4mm 间距，上限 256 点）；新增 `_inner_wall_offset(s)`——向局部曲率中心（凹侧）偏移、仅取横向分量（端点单边差分的切向伪偏移被投影掉）、随曲率增长并夹在 `wall_lean`；`NavigationEngine` 新增 `wall_lean`（默认 2.5mm）参数 |
| `godot_client/scripts/guidewire_renderer.gd` | 导丝由细线改为**管状网格**：平行传输坐标架沿折线挤出 8 边圆环、生成三角面与法线；受光金属材质（`metallic=0.6`/`roughness=0.35`/双面），曲率经明暗显现；保留尖端球 |

### 19.3 关键实现说明

1. **全长显示**：`s_tip` 处按 ~4mm 间距从 0 采样到 tip，导丝随路径在每个弯处曲折；点数随插入深度增长、上限 256，带宽可控（每帧 ≤ ~5KB）。
2. **贴内壁**：用 `s±10mm` 三点估局部凹向，**投影掉切向分量**只留横向，故直段与端点零偏移、仅真实弯曲处向内壁靠，幅度随曲率增长并夹在 `wall_lean`（2.5mm），不会穿出管腔。
3. **消锯齿**：管状网格用平行传输坐标架避免扭转；受光材质让弯曲经明暗可见，区别于原 unshaded 细线"锯齿/涂鸦"观感。
4. **物理模式同样受益**：low_tort 物理会话的 `bodies`（MuJoCo geom）也走同一管状渲染，导丝更立体。

### 19.4 端到端实测（case_001 guided）

```text
推进至 progress 0.528：render bodies 152，首点贴入口[-0.0039 -0.2685 0.291]、
末点在 tip[-0.5773 -0.1965 0.3191]，端到端 0.579m（全长随血管曲折）
中段弯曲处 inner-wall 偏移 ~0.77mm（直段/端点为 0）
```

### 19.5 测试与验收

| 文件 | 内容 |
|------|------|
| `tests/test_navigation_engine.py` | `TestGuidedMode` 更新 `test_render_bodies_span_full_inserted_length`（首点≈入口、末点≈tip、直段无横移）；新增 `test_inner_wall_lean_offsets_on_bend`（L 形路径拐角处偏移>0 且≤wall_lean） |

```powershell
$env:MUJOCO_GL="glfw"
.\.venv\Scripts\python.exe -m pytest tests/test_websocket.py tests/test_navigation_engine.py tests/test_risk_assessor.py tests/test_services_api.py tests/test_vpp_path_planner.py tests/test_vhacd.py -q
```

实测结果：**105 passed**（较阶段十三 104 新增 1：inner-wall lean；render 测试改名），无回归。
客户端为 GDScript，本环境无 Godot 编辑器，管状渲染仅离线核查 API/坐标帧（同既往做法）。

### 19.6 可调参数（实机手感）

- `wall_lean`（后端，默认 2.5mm）：转弯贴壁幅度；0 = 走中心线。
- `wire_radius`（客户端，默认 1.2mm）、`wire_sides`（默认 8）：导丝粗细与圆润度。
- `advance_per_step`（后端，默认 10mm）：循路推进速度。

### 19.7 已知限制

- 仍是运动学循路，**不计算真实接触力**（方案 A 的本质取舍）；"贴内壁"为基于曲率的几何近似而非碰撞解算。
- 如需真实接触力训练，按 §18.3 方案 B/C 另立项。

---

## 二十、阶段十五执行记录：内窥镜管腔可见性

### 20.1 问题

内窥镜（第一人称）视角下看不到血管腔——血管"透明"了。根因：血管材质为半透明
（alpha 0.28）+ 双面渲染，从内部看会直接穿透过去，没有可见内壁（§15.5 早已标注的限制）。

### 20.2 修复

| 文件 | 改动 |
|------|------|
| `godot_client/scripts/main_controller.gd` | 血管材质改为**按视角模式切换**：概览/跟随用原半透明（外部可见导丝）；内窥镜切**不透明内壁材质**（双面，Godot 自动翻转背面法线；加**适度自发光**使内壁柔和可见）。`_set_vessel_interior()` 在 `_set_camera_mode` 中按 `mode==ENDOSCOPE` 切换 |

### 20.3 关键说明

1. **按模式换材质**：不透明只在内窥镜启用——概览/跟随仍半透明以便从外部看导丝；切视角时 `_set_vessel_interior(mode==ENDOSCOPE)` 自动换。
2. **内壁可见用自发光、不用头灯**：cm 级管腔里点光源（`OmniLight3D`）离壁太近，按平方反比会**爆白**；改为内壁材质带适度 `emission`（暗红、energy 0.35）+ 环境光，柔和均匀可见，不爆白也不全黑。双面渲染（`CULL_DISABLED`）保证内壁法线正确。

### 20.4 可调参数

- 内壁材质 `emission`（默认暗红 0.45,0.16,0.15）/ `emission_energy_multiplier`（默认 0.35）：管腔亮度，调这个控制明暗。
- 内壁 `albedo`（默认暗红 0.7,0.28,0.26）/ `roughness`（0.85）：管腔观感。

纯客户端渲染改动，无后端/测试影响（维护套件维持 105 passed）；本环境无 Godot 编辑器，仅离线核查。
